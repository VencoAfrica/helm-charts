apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "venco-bench.fullname" . }}-test-connection"
  labels:
    {{- include "venco-bench.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "venco-bench.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
